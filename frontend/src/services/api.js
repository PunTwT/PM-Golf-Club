// api.js
// Centralised API configuration — exports the base URL used by all service modules

// Base URL for all backend API requests
// Change this value to switch between development and production environments
const API_BASE_URL = "http://localhost:5000/api";

export default API_BASE_URL;
